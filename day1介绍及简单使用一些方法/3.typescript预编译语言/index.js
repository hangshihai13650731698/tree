// 一保存就会生成js文件
(function () {
    var num = 1;
    var bool = true;
    var arr = [1, 2, 3, 4];
    var arr2 = ['1', '2'];
    var arr3 = ['1', 2, true];
    var fn = function (num1, num2) {
        return num1 + num2 + '1223';
    };
    fn(1, 2);
})();
